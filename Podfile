# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NeoProducts-VIP' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NeoProducts-VIP
  pod 'Alamofire'
    pod 'SDWebImage'
  target 'NeoProducts-VIPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NeoProducts-VIPUITests' do
    # Pods for testing
  end

end
